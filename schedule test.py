# test your code here

def schedulecheck
    get info from db
    append chatid to list
    check with db for every chat id
    now = datetime.now
    if (today is day returned in file)
        if (today time is time returned in file)
            (send reminder message)
    pass